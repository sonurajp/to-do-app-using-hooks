npm install uuid
1. setedit now contain the item that we want to edit that is the old title which we are going to edit
2. the reason why we are checking if it is null is because its null b4 clicking th e edit button 
it is actually checking if we have clicked the edit button or not if this condition was not checked when it render it will look for edit.title
 when is not there thus giving us error